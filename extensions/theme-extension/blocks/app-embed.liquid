{% comment %} Free Shipping Bar - App Embed {% endcomment %}
<div id="free-shipping-bar-embed"></div>
{{ 'shipping-bar.css' | asset_url | stylesheet_tag }}
<script>
  window.freeShippingThreshold = {{ block.settings.threshold | default: 5000 }};
  window.freeShippingMessage = {{ block.settings.progress_message | json }};
  window.freeShippingSuccessMessage = {{ block.settings.success_message | json }};
  window.freeShippingBarColor = {{ block.settings.bar_color | json }};
</script>
<script src="{{ 'shipping-bar.js' | asset_url }}" defer></script>
{% schema %}
{
  "name": "Free Shipping Bar",
  "target": "body",
  "settings": [
    {
      "type": "number",
      "id": "threshold",
      "label": "Free shipping threshold (cents)",
      "default": 5000
    },
    {
      "type": "text",
      "id": "progress_message",
      "label": "Progress message",
      "default": "Add [amount] more for free shipping!"
    },
    {
      "type": "text",
      "id": "success_message",
      "label": "Success message",
      "default": "You've unlocked free shipping! 🎉"
    },
    {
      "type": "color",
      "id": "bar_color",
      "label": "Bar color",
      "default": "#008060"
    }
  ]
}
{% endschema %}
